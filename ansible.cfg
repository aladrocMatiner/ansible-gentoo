[defaults]
inventory = ansible/inventory/live.yml
roles_path = ansible/roles
retry_files_enabled = False
# Keep host key checking enabled by default. Host-to-live-ISO VM wrappers may
# disable it per invocation because the official live ISO SSH host key is
# temporary and changes after reboot.
