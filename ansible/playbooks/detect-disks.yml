---
- name: Read-only disk detection
  hosts: gentoo_live
  gather_facts: true
  roles:
    - role: common/disk_detection
    - role: common/install_state
      vars:
        install_state_phase: disk-detection
