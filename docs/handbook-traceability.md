# Gentoo Handbook Traceability

This report maps `gentoo-ai-installer` workflows to the official Gentoo AMD64 Handbook baseline.

Handbook baseline: https://wiki.gentoo.org/wiki/Handbook:AMD64

Generated by `make handbook-trace` from `config/handbook-traceability.json`.

## Metadata Fields

Each entry records phase, Handbook section, Makefile targets, Ansible roles, implementation status, safety gates, validation evidence, project deviations, and OpenSpec references.

## Phase Map

| Phase | Status | Handbook Section | Make Targets | Ansible Roles |
| --- | --- | --- | --- | --- |
| Live ISO preflight | implemented | Choosing installation media, booting the installation media, network setup, and system clock checks | `make ansible-live-preflight`<br>`make local-live-preflight`<br>`make vm-bootstrap-ssh`<br>`make vm-ansible-ping` | `common/live_preflight` |
| Disk discovery and planning | implemented | Preparing the disks | `make detect-disks`<br>`make local-detect-disks`<br>`make install-plan`<br>`make local-install-plan`<br>`make partition-plan`<br>`make local-partition-plan`<br>`make mount-plan`<br>`make filesystem-plan` | `common/disk_detection`<br>`common/disk_safety`<br>`common/install_plan`<br>`common/partition_plan`<br>`common/mount_plan`<br>`common/filesystem_plan` |
| Partition apply | implemented | Preparing the disks | `make partition` | `common/partitioning` |
| Filesystem creation | implemented | Creating filesystems | `make format` | `common/filesystem` |
| Mount target filesystems | implemented | Mounting the root partition | `make mount-target` | `common/mount_target` |
| Stage3 install | implemented | Installing the Gentoo installation files | `make stage3-install` | `common/stage3` |
| Chroot preparation | implemented | Chrooting | `make prepare-chroot` | `common/chroot` |
| Portage baseline | implemented | Configuring Portage | `make configure-portage` | `common/portage` |
| System identity and fstab | implemented | Configuring the system | `make configure-system`<br>`make generate-fstab` | `common/locale_timezone_hostname`<br>`common/fstab` |
| Kernel installation | implemented | Configuring the Linux kernel | `make install-kernel` | `common/kernel` |
| System packages and services | implemented | Installing system tools and configuring networking | `make install-system-packages` | `common/ssh`<br>`common/package_install`<br>`init/openrc`<br>`init/systemd` |
| Users and access | implemented | User administration | `make configure-users` | `common/users` |
| Bootloader | implemented | Configuring the bootloader | `make install-bootloader` | `common/bootloader` |
| Final checks and reboot readiness | implemented | Rebooting the system | `make final-checks`<br>`make install-audit` | `common/final_checks` |
| Local libvirt validation | implemented | Project-specific validation harness; not a Handbook installation step | `make vm-check`<br>`make vm-disk`<br>`make vm-define`<br>`make vm-start`<br>`make vm-bootstrap-ssh`<br>`make vm-ansible-ping` | _None_ |

## Details

### Live ISO preflight

- Status: `implemented`
- Handbook section: Choosing installation media, booting the installation media, network setup, and system clock checks

Make targets:
- make ansible-live-preflight
- make local-live-preflight
- make vm-bootstrap-ssh
- make vm-ansible-ping

Ansible roles:
- common/live_preflight

Safety gates:
- read-only
- official live ISO evidence
- amd64
- UEFI
- network/DNS/time checks

Validation evidence:
- stdout preflight report
- install state checkpoint when run through Makefile

Project-specific deviations:
- Uses SSH-driven Ansible control from the operator machine instead of manual console-only commands.

OpenSpec references:
- define-remote-network-ansible-control-plane
- implement-live-iso-ansible-preflight
- implement-live-iso-local-ansible-control-plane

### Disk discovery and planning

- Status: `implemented`
- Handbook section: Preparing the disks

Make targets:
- make detect-disks
- make local-detect-disks
- make install-plan
- make local-install-plan
- make partition-plan
- make local-partition-plan
- make mount-plan
- make filesystem-plan

Ansible roles:
- common/disk_detection
- common/disk_safety
- common/install_plan
- common/partition_plan
- common/mount_plan
- common/filesystem_plan

Safety gates:
- no default install disk
- explicit INSTALL_DISK for disk plans
- mounted descendant rejection
- read-only planning

Validation evidence:
- disk inventory
- partition plan
- mount plan
- filesystem plan
- install state resume checkpoint

Project-specific deviations:
- Plans both ext4 and approved Btrfs subvolume variants; no LUKS or swap partition in current scope.

OpenSpec references:
- implement-ansible-disk-detection-and-install-plan
- implement-ansible-partition-plan
- implement-ansible-mount-plan
- implement-ansible-filesystem-plan
- define-btrfs-subvolume-and-snapshot-policy
- implement-live-iso-local-ansible-control-plane

### Partition apply

- Status: `implemented`
- Handbook section: Preparing the disks

Make targets:
- make partition

Ansible roles:
- common/partitioning

Safety gates:
- INSTALL_DISK required
- I_UNDERSTAND_THIS_WIPES_DISK=yes required
- shared disk safety
- destructive command preview

Validation evidence:
- logs/install-runs/<run-id>/partition/before.json
- logs/install-runs/<run-id>/partition/after.json

Project-specific deviations:
- Uses guarded sgdisk argv commands for a GPT ESP/root layout instead of interactive Handbook partitioning.

OpenSpec references:
- implement-ansible-partition-apply

### Filesystem creation

- Status: `implemented`
- Handbook section: Creating filesystems

Make targets:
- make format

Ansible roles:
- common/filesystem

Safety gates:
- shared disk safety
- destructive confirmation
- partition path validation
- mounted filesystem rejection

Validation evidence:
- logs/install-runs/<run-id>/filesystem/before.json
- logs/install-runs/<run-id>/filesystem/after.json

Project-specific deviations:
- Supports ext4 or Btrfs with approved subvolumes; Btrfs root mounts use subvol=@

OpenSpec references:
- implement-ansible-filesystem-apply
- define-btrfs-subvolume-and-snapshot-policy

### Mount target filesystems

- Status: `implemented`
- Handbook section: Mounting the root partition

Make targets:
- make mount-target

Ansible roles:
- common/mount_target

Safety gates:
- explicit target mount
- approved partition paths
- idempotent mount validation
- Btrfs subvolume validation

Validation evidence:
- logs/install-runs/<run-id>/mount-target/before.json
- logs/install-runs/<run-id>/mount-target/after.json

Project-specific deviations:
- Uses /mnt/gentoo/boot/efi before chroot to produce target /boot/efi.

OpenSpec references:
- implement-ansible-mount-target

### Stage3 install

- Status: `implemented`
- Handbook section: Installing the Gentoo installation files

Make targets:
- make stage3-install

Ansible roles:
- common/stage3

Safety gates:
- mounted target root required
- amd64 variant match
- checksum verification
- signature policy

Validation evidence:
- logs/install-runs/<run-id>/stage3/verification.json
- logs/install-runs/<run-id>/stage3/extraction.json

Project-specific deviations:
- Stage3 variant is selected from PROFILE=openrc|systemd and signatures fail closed by policy.

OpenSpec references:
- implement-stage3-signature-policy
- implement-ansible-stage3-install

### Chroot preparation

- Status: `implemented`
- Handbook section: Chrooting

Make targets:
- make prepare-chroot

Ansible roles:
- common/chroot

Safety gates:
- target root markers
- pseudo-filesystem mount checks
- DNS validation

Validation evidence:
- logs/install-runs/<run-id>/chroot/prepare.json

Project-specific deviations:
- Uses Ansible-controlled chroot preparation while keeping target root under /mnt/gentoo.

OpenSpec references:
- implement-ansible-chroot-preparation

### Portage baseline

- Status: `implemented`
- Handbook section: Configuring Portage

Make targets:
- make configure-portage

Ansible roles:
- common/portage

Safety gates:
- conservative make.conf
- official repo
- profile match
- GURU disabled in installed system

Validation evidence:
- logs/install-runs/<run-id>/portage/baseline.json

Project-specific deviations:
- Keeps v1 minimal and avoids broad @world updates.

OpenSpec references:
- implement-ansible-portage-baseline
- define-portage-world-update-policy

### System identity and fstab

- Status: `implemented`
- Handbook section: Configuring the system

Make targets:
- make configure-system
- make generate-fstab

Ansible roles:
- common/locale_timezone_hostname
- common/fstab

Safety gates:
- target root validation
- UUID validation
- Btrfs subvolume fstab validation

Validation evidence:
- logs/install-runs/<run-id>/system-config/identity.json
- logs/install-runs/<run-id>/fstab/fstab.json

Project-specific deviations:
- Generates UUID-based fstab for ext4 or the approved Btrfs layout.

OpenSpec references:
- implement-locale-timezone-hostname
- implement-ansible-fstab-generation

### Kernel installation

- Status: `implemented`
- Handbook section: Configuring the Linux kernel

Make targets:
- make install-kernel

Ansible roles:
- common/kernel

Safety gates:
- fstab-derived root UUID
- Btrfs rootflags validation
- no LUKS/resume kernel args

Validation evidence:
- logs/install-runs/<run-id>/kernel/kernel.json

Project-specific deviations:
- Uses gentoo-kernel-bin with installkernel and dracut support instead of manual kernel compilation.

OpenSpec references:
- implement-ansible-kernel-install
- define-boot-kernel-commandline-policy

### System packages and services

- Status: `implemented`
- Handbook section: Installing system tools and configuring networking

Make targets:
- make install-system-packages

Ansible roles:
- common/ssh
- common/package_install
- init/openrc
- init/systemd

Safety gates:
- shared package framework
- init-specific service enablement
- NetworkManager policy

Validation evidence:
- logs/install-runs/<run-id>/system-packages/packages-services.json

Project-specific deviations:
- Uses NetworkManager for v1 and init-specific OpenRC/systemd service enablement.

OpenSpec references:
- implement-ansible-system-packages-and-services
- define-installed-ssh-policy
- define-installed-time-sync-policy

### Users and access

- Status: `implemented`
- Handbook section: User administration

Make targets:
- make configure-users

Ansible roles:
- common/users

Safety gates:
- ADMIN_USER required
- secret file inputs only
- no_log for password hashes
- sudo policy validation

Validation evidence:
- logs/install-runs/<run-id>/users/users-access.json

Project-specific deviations:
- Optional password hashes and authorized_keys are controller-local files and never committed.

OpenSpec references:
- implement-ansible-users-and-access
- implement-secret-input-policy

### Bootloader

- Status: `implemented`
- Handbook section: Configuring the bootloader

Make targets:
- make install-bootloader

Ansible roles:
- common/bootloader

Safety gates:
- UEFI required
- EFI mount validation
- INSTALL_DISK required
- I_UNDERSTAND_BOOTLOADER_CHANGES=yes required

Validation evidence:
- logs/install-runs/<run-id>/bootloader/grub.json

Project-specific deviations:
- Uses GRUB UEFI and /boot/efi as the target EFI mountpoint.

OpenSpec references:
- implement-ansible-bootloader-grub

### Final checks and reboot readiness

- Status: `implemented`
- Handbook section: Rebooting the system

Make targets:
- make final-checks
- make install-audit

Ansible roles:
- common/final_checks

Safety gates:
- read-only validation
- no automatic reboot
- secret-safe report and audit bundle

Validation evidence:
- logs/install-runs/<run-id>/final-checks/reboot-readiness.json
- logs/install-runs/<run-id>/audit-bundle/manifest.json

Project-specific deviations:
- Project requires explicit manual reboot after final checks and supports audit bundle generation.

OpenSpec references:
- implement-ansible-final-checks-and-reboot-readiness
- implement-install-audit-bundle

### Local libvirt validation

- Status: `implemented`
- Handbook section: Project-specific validation harness; not a Handbook installation step

Make targets:
- make vm-check
- make vm-disk
- make vm-define
- make vm-start
- make vm-bootstrap-ssh
- make vm-ansible-ping

Ansible roles:
_None._

Safety gates:
- qcow2 only
- project-local VM artifacts
- no host block devices
- official ISO

Validation evidence:
- libvirt VM state
- Ansible ping output
- install state/audit evidence from VM runs

Project-specific deviations:
- Disposable VM testing is required before real hardware use but does not replace Handbook target behavior.

OpenSpec references:
- migrate-qemu-workflow-to-libvirt-virsh
- implement-libvirt-install-test-matrix

