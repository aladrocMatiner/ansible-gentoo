# Downloaded Gentoo release artifacts are fetched inputs, not source.
gentoo.iso/

# Local secret files must never be committed.
.env
.env.*
!.env.example

# Runtime logs and temporary files.
logs/
tmp/
